FROM codekoala/arch
MAINTAINER Josh VanderLinden <codekoala@gmail.com>

RUN pacman -Sy --noconfirm --needed unzip \
        fontconfig freetype2 libcups \
        ttf-dejavu jre7-openjdk

ENTRYPOINT ["/opt/Sencha/Cmd/{CMD_VER}/sencha"]

RUN curl -o /cmd.run.zip http://cdn.sencha.com/cmd/{CMD_VER}/SenchaCmd-{CMD_VER}-linux-amd64.sh.zip && \
    unzip -p /cmd.run.zip > /cmd-install.run && \
    chmod +x /cmd-install.run && \
    /cmd-install.run -q -dir /opt/Sencha/Cmd/{CMD_VER} && \
    install -dm777 -o root -g root /opt/Sencha/Cmd/repo && \
    rm /cmd-install.run /cmd.run.zip
